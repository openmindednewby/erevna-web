/** Shared numeric defaults for scale/rating/nps question types. */

/** Default discrete rating (Likert) bounds. */
export const RATING_MIN = 1;
export const RATING_MAX = 5;

/** NPS is always a fixed 0-10 scale. */
export const NPS_MIN = 0;
export const NPS_MAX = 10;

/** Default continuous linear-scale bounds. */
export const LINEAR_SCALE_MIN = 1;
export const LINEAR_SCALE_MAX = 10;

/** Default step for discrete scales. */
export const SCALE_STEP_DEFAULT = 1;
